# Direct Dark-Moon MCP dependencies. The image installs requirements.lock,
# which pins the complete transitive environment reproduced by CI.
fastmcp==3.4.5
docker==7.2.0
pydantic==2.13.4
python-dotenv==1.2.2
cryptography==50.0.0
